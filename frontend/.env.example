# Local development defaults to the FastAPI service on port 8000.
# Set this to the deployed backend URL in Vercel, for example:
# VITE_API_BASE_URL=https://aurora-api.onrender.com
VITE_API_BASE_URL=http://localhost:8000
